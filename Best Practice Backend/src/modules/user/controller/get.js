import { getData } from "../services/get"

export const get = (req, res) => {
    try {
       const user = getData() 
    } catch (error) {
        
    }
}

// service lia aur jhn se reuest aai ha front end ko wapis dia